services:
  static_serve:
    container_name: static_serve
    # image: ghcr.io/endikau/nd_docker-static_serve:latest
    image: nd_docker-static_serve:local  
    # image: static_serve:latest
    # build:
    #   context: ./runtime
    #   dockerfile: static_serve.Dockerfile
#     pull_policy: never
#     working_dir: /nd.site
#     volumes:
#       - ~/Documents/endikau/nd.site:/project
#       - ~/Documents/endikau/.cache/container:/root/.cache
#       - ~/Documents/endikau/.cache/static_site/container:/root/.cache
#     command: |
#       Rscript --vanilla setup_envs.R
#       sleep infinity
#       quarto render
